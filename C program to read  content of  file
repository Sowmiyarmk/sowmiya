#include <stdio.h>
#include <stdlib.h>

int main()
{
    FILE *fp;
    int ch;
    fp=fopen("data.txt","r");
    if(fp==NULL)
    {
        printf("file cannot be opened");
        return 1;
    }
    while((ch=getc(fp))!=EOF)
        putchar(ch);
    fclose(fp);
    return 0;
}
